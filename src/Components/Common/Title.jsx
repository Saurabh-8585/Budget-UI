import React from 'react'

const Title = ({ title }) => {
    return (
        <h5 className="mb-2 font-bold text-xl tracking-tight text-gray-900 dark:text-white">
            {title}
        </h5>
    )
}

export default Title